/** Slug URL alinhado ao server action `salvarNovaAnaliseEditorialAction`. */
export function normalizarSlugEditorial(raw: string): string {
  return raw
    .trim()
    .toLowerCase()
    .replace(/\s+/g, "-")
    .replace(/[^a-z0-9-]/g, "")
    .replace(/-+/g, "-")
    .replace(/^-|-$/g, "");
}
